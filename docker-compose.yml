services:
  web:
    build: .
    ports:
      - "5050:5050"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./config:/app/config:ro
    environment:
      - TOSMOD_DB_PATH=/app/data/tosmod.db
